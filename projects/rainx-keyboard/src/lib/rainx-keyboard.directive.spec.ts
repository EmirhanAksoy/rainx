import { RainxKeyboardDirective } from './rainx-keyboard.directive';

describe('RainxKeyboardDirective', () => {
  it('should create an instance', () => {
    const directive = new RainxKeyboardDirective();
    expect(directive).toBeTruthy();
  });
});
